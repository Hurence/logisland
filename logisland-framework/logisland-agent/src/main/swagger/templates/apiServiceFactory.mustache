package {{package}}.factories;

import {{package}}.{{classname}}Service;
import {{package}}.impl.{{classname}}ServiceImpl;

import com.hurence.logisland.kafka.registry.KafkaRegistry;

{{>generatedAnnotation}}
public class {{classname}}ServiceFactory {
    private static {{classname}}Service service = null;

    public static {{classname}}Service get{{classname}}(KafkaRegistry kafkaRegistry) {
        if (service == null) {
            service = new {{classname}}ServiceImpl(kafkaRegistry);
        }
        return service;
    }
}